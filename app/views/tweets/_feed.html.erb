<% @tweets.each do |t| %>
   <div class="d-block ">
   <div class="d-flex justify-content-between">
   <p><%= t.author.username %></p>
	<p><%= t.created_at.strftime("#{t.created_at.day.ordinalize} %B %Y") %></p>
   </div>

	<p><%= t.text %></p>
	</div>
<% end %>